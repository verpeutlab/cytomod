language: bash
# command to install dependencies
sudo: true
cache:
    - apt
    - pip
install:
    - sudo apt-get install bedtools libhdf5-dev
    - sudo pip install -r tests/requirements.txt
# command to run tests
script:
    - bash tests/testCytomod.sh

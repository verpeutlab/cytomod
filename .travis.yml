language: bash
# command to install dependencies
sudo: true
cache:
    - apt
    - pip
install:
    - sudo apt-get install bedtools libhdf5-serial-dev
    - cat tests/requirements.txt | xargs -n 1 pip install
# command to run tests
script:
    - bash tests/testCytomod.sh
